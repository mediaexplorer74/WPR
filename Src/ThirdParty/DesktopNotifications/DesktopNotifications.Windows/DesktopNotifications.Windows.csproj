<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

  <PropertyGroup>
	<LangVersion>8.0</LangVersion>
	<Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup Condition="'$(OS)' == 'Windows_NT' ">
		<Compile Remove="NullImpl_WindowsNotificationManager.cs" />
	</ItemGroup>

	<ItemGroup Condition="'$(OS)' != 'Windows_NT' ">
		<Compile Remove="ShellLink.cs" />
		<Compile Remove="WindowsNotificationManager.cs" />
		<Compile Remove="WindowsApplicationContext.cs" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\DesktopNotifications\DesktopNotifications.csproj" />
    <PackageReference Condition="'$(TargetFramework)' == 'Windows_NT' " Include="Microsoft.Toolkit.Uwp.Notifications"  Version="7.1.0" />
		<PackageReference Condition="'$(TargetFramework)' == 'Windows_NT' " Include="Microsoft.Windows.SDK.Contracts" Version="10.0.22621.2" />
	</ItemGroup>

</Project>